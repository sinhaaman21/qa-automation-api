stages:
  - test

test:
  ##image: node latest
  image: cypress/included:9.5.0
  stage: test
  tags: 
    - awsgitrunner
  script:
    # install dependencies
    - npm ci
    # start the server in the background
    - npm Xvfb :99 &
    # run Cypress tests
    - npm run e2e